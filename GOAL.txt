Потрібна саме програмна реалізація (веб-інтеграція відкладається на невідомий термін, і розглядуватись поки НЕ буде)

Написання на мові C відмовлено, через низьку інтеграцію та низькорівневу реалізацію (причина об'ємної роботи)

1. На старті може бути й без GUI, але обов'язково написане на C++
2. Далі обов'язково потрібно буде написати просте GUI
3. Придумати реалізацію ключів та платних доступів (в Chat GPT спитати)

Уявлення щодо GUI: https://www.figma.com/design/BYKSbrTpMLJVrLsJNy1y4g/Untitled?node-id=0-1&t=Fs7CA83NDBXZ7sDJ-1

4. Потрібно реалізувати автомат (часу інтеграції машинного навчання може бути проблематичним, через високий об'єм пам'яті: але потрібно буде спитати у чата GPT)
Щодо автоматів: поки залишаємось на ручному вводі всіх даних, також потрібне GUI (2 макет) а поки націлимось на термінал-вводі [ Тобто GUI буде складатись із 2 вікон (2 режимів)]