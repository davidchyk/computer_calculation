1. Короче це вікно, яке має декілька полів вводу

    1. радіо вибір вводу даних про функцію у вигляді виразу, чи або списком наборів, при якому функція набуває значення 1

    типу якщо змінюється вибір, то змінюється і контент програми

    Якщо вибраний expression mode 

    то має бути поле для вводу виразу булеовї функції. Я це бачу просто як text field, де там &, |, ()

    далі має бути ввід базису, і довзолені лише 8 форм, інші ні, а саме:

    AND/OR
    NAND/NAND
    OR/NAND
    NOR/OR
    OR/AND
    NOR/NOR
    AND/NOR
    NAND/AND

    можна зробити випадаючим списком

    і десь має бути кнопка Calculate або Analyze 

    а якщо вибраний 2 режим, то має бути інші поля

    1. Вводиться число аргументів. Це можна зробити якось особливим шляхом, типу від 1 до 9 лише дозволені

    2. Поле однорядкового textfield де через кому пишутться числа від 0 до 2^число_аргументів-1 максимум.

    3. Далі знову базис вводиться

    Але тут проблема. Базис має складатись не тільки із дозволеної із 8 форм, але й числа. Тобто не 

    AND/OR

    а, наприклад

    2AND/2OR

    тобто від користувача потрібно ще приймати якось два числа, окрім як базису. НЕ знаю, як по вебу це можна зробити, чи ні, але я б зробив так, що буде випадаючий список форм, де вони будуть як

    [ ]AND/[ ]OR

    а потім в [ ] користувач просто введе число (від 2 до 9)

    Також має бути для обох режимів користувацькі аргументи. Це єдине поле яке не є обов'язовим.

2. Стиль має бути мінімалістичний, темний, професійний. Можеш vscode стиль, spacex, apple стиль (тобто різні стилі робити, а я виберу який). Тобто можеш додати кнопку перемиканння

3. 


Також дм